<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boot.data.mapper.MemberMapperInter">
	<insert id="insertMember" parameterType="member">
		insert into member (name,id,pass,photo,email,gaipday) values (#{name},#{id},#{pass},#{photo},#{email},now())
	</insert>
	<select id="getAllMemebers" resultType="member">
		select * from member order by num
	</select>
	<select id="getSearchId" parameterType="String" resultType="int">
		select count(*) from member where id=#{id}
	</select>
	<delete id="deleteMember" parameterType="String">
		delete from member where num=#{num}
	</delete>
	<select id="getDataByNum" parameterType="String" resultType="member">
		select * from member where num=#{num}
	</select>
	<update id="updatePhoto" parameterType="Map">
		update member set photo=#{photo} where num=#{num}
	</update>
	<update id="updateMember" parameterType="member">
		update member set name=#{name},email=#{email} where num=#{num}
	</update>
</mapper>